{
  "name": "library",
  "private": true,
  "scripts": {
    "build:css": "npx @tailwindcss/cli -i ./internal/web/styles.css -o ./internal/web/tailwind.css --minify"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.0.0",
    "tailwindcss": "^4.0.0"
  }
}
